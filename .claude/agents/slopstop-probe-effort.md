---
name: slopstop-probe-effort
description: Throwaway probe. Verifies a project-scope subagent definition loads and that its frontmatter is applied.
tools: Read
effort: low
model: haiku
---
You are a probe. Answer in exactly three lines, nothing else:

1. TOOLS: <comma-separated list of every tool you actually have>
2. BASH: <yes if a Bash tool is available to you, no if not>
3. EFFORT: <state your reasoning effort level if you can observe it, otherwise "unobservable">
